\chapter{Evaluación}\label{chapter:evaluacion}

\section{Metodología de evaluación}

Este capítulo tiene como objetivo contrastar empíricamente el sistema implementado en el Capítulo~\ref{chapter:desarrollo}
frente a los objetivos específicos definidos en la Sección~\ref{section:objetivos-especificos}. La evaluación se
organiza en dos niveles complementarios:

\boldparagraph{Evaluación por módulos}{
    cada etapa del \englishText{pipeline}: transcripción (\englishText{ASR}), extracción de entidades
    (\englishText{NER}) y normalización terminológica
    (\englishText{SNOMED CT}/\englishText{CIE-10-ES}) se evalúa de forma independiente sobre el
    conjunto de datos descrito en la Sección~\ref{section:conjunto_datos_evaluacion}, empleando
    métricas cuantitativas estándar como error por palabra (\englishText{WER}), precisión, \textit{recall} y \englishText{F1}.
}

\boldparagraph{Evaluación \englishText{end-to-end} asistida por IA}{
    el sistema completo se evalúa de extremo a extremo, desde el audio de entrada hasta los entregables
    finales (informe clínico, \englishText{JSON FHIR}), utilizando un modelo de lenguaje como juez automático
    (\englishText{LLM-as-judge}) que contrasta cada artefacto de salida contra el texto de referencia.
    Esta evaluación se realiza sobre las dos modalidades de funcionamiento del sistema:
    (\textit{batch} y \textit{streaming}), descritas en la Sección~\ref{section:evaluacion_e2e}.
}

\section{Conjunto de datos de evaluación}\label{section:conjunto_datos_evaluacion}

Para garantizar la reproducibilidad de las métricas obtenidas, se construyó un conjunto de evaluación 
compuesto por 10 dictados médicos de referencia (indexados del 1 al 10). Estos fueron seleccionados 
de forma aleatoria a partir de los 1.000 casos clínicos en español que integran el corpus 
\englishText{SPACCC} (\englishText{Spanish Clinical Case Corpus}), detallado en el 
Capítulo~\ref{chapter:marco_teorico_y_referencial}. Con el objetivo de llevar a cabo las evaluaciones 
a partir de la muestra seleccionada, se definen los siguientes componentes fundamentales:

\boldparagraph{Texto de referencia}{
    Texto original destinado a la lectura en voz alta y almacenado para cada caso de prueba.
}

\boldparagraph{Entidades de referencia}{
    Anotación de las entidades clínicas relevantes, tales como diagnósticos, procedimientos, fármacos, 
    dosis y alergias. Para la etiquetar y señalar las entidades de referencia, 
    se empleó el modelo de lenguaje Claude 4.5 Opus actuando como agente de anotación automática. Los 
    detalles correspondientes al diseño y ejecución de los \englishText{prompts} utilizados se encuentran 
    disponibles en el (Anexo~\ref{anexo:claude_opus_4_5}).
}

\boldparagraph{Entidades normalizadas a SNOMED CT y CIE-10-ES}{
    Vinculación y codificación de las entidades de referencia con los vocabularios estándares 
    \englishText{SNOMED CT} y \englishText{CIE-10-ES}, un proceso realizado a través de los buscadores 
    de términos médicos provistos por \englishText{SNOMED CT} y \englishText{CIE-10-ES}.
}

Este conjunto de tres elementos constituye el estándar de referencia (\englishText{gold standard}) 
de la presente evaluación, frente al cual se contrastan los resultados generados por el sistema 
para determinar su rendimiento y calidad.


\section{Evaluación de los módulos individuales}\label{section:evaluacion_modulos}

Esta sección evalúa de forma independiente cada una de las tres etapas técnicas del
\englishText{pipeline} de procesamiento: transcripción \englishText{ASR}, extracción de entidades
\englishText{NER} y normalización terminológica \englishText{SNOMED CT}/\englishText{CIE-10-ES}.

\subsection{Transcripción \englishText{ASR}}\label{subsection:evaluacion_asr}

El módulo \englishText{ASR} se evaluó calculando la tasa de error de palabra (\englishText{Word Error
Rate, WER}) entre la transcripción producida por \englishText{faster-whisper} y el \textit{gold
standard} de cada uno de los 10 casos, sobre las dos modalidades de funcionamiento del sistema
(\textit{batch} y \textit{streaming}). Dado que el modelo \englishText{ASR} y el
audio de entrada son idénticos en ambas modalidades. La diferencia entre ellas reside únicamente
en la forma en que el \englishText{gateway} agrupa y envía los fragmentos de audio, no en el motor de
transcripción, se reportan los resultados promediados de los informes de evaluación de ambas
modalidades en la Tabla~\ref{tab:asr_promedios}.

Adicionalmente a la métrica \englishText{WER}, se le asigna a la transcripción de cada caso una
puntuación en una escala de 1 a 5. Esta puntuación, se define a partir
del porcentaje de palabras transcritas correctamente, según la Tabla~\ref{tab:asr_escala}.

\input{tablas/capitulo_evaluacion/tabla_asr_escala.tex}

La Tabla~\ref{tab:asr_scores_detalle} recoge, para cada uno de los 10 casos y ambas modalidades, el
porcentaje de palabras correctas obtenido frente al \textit{gold standard} y la puntuación que se
deriva de él según la escala anterior.

\input{tablas/capitulo_evaluacion/tabla_asr_scores_detalle.tex}

\input{tablas/capitulo_evaluacion/tabla_asr_promedios.tex}

El \englishText{WER} medio combinado ($\approx$7.2\,\%) se encuentra por debajo del umbral definido
en el objetivo específico~1 ($\le$10--12\,\%, Sección~\ref{section:objetivos-especificos}), lo que
permite considerar este objetivo cumplido. El comportamiento del módulo es, además, consistente
entre modalidades: en 9 de los 10 casos el \englishText{WER} se mantiene en un rango bajo
($\approx$0.6--13.6\,\%), con errores menores que en general no alteran el sentido clínico de la
nota (sustituciones fonéticamente similares, fragmentación de términos compuestos). El caso
\texttt{Test9} en modo \textit{batch} ($\approx$20\,\%\,\englishText{WER}) constituye la única
excepción relevante, asociada a una posible contaminación cruzada de audio.

\subsection{Extracción de entidades \englishText{PLN}/\englishText{NER}}\label{subsection:evaluacion_pln}

El módulo \englishText{NER} se evaluó de forma independiente a la evaluación
\englishText{end-to-end} de la Sección~\ref{section:evaluacion_e2e}, calculando precisión,
\textit{recall} y F1 sobre los 10 casos del conjunto de evaluación
(Sección~\ref{section:conjunto_datos_evaluacion}), comparando las entidades detectadas por el
\textit{pipeline} de extracción frente a las entidades de referencia anotadas para cada
caso. La comparación se realiza mediante coincidencia exacta de texto (normalizado a minúsculas)
entre el conjunto de entidades detectadas y el conjunto de entidades de referencia: toda entidad
detectada que no coincide literalmente con ninguna entidad de referencia se cuenta como falso
positivo, y toda entidad de
referencia no cubierta por ninguna detección se cuenta como falso negativo.

A partir de esta comparación se definen los tres conjuntos sobre los que se calculan las métricas de
esta sección:

\boldparagraph{Verdaderos positivos (\textit{true positives}, TP)}{
    entidades detectadas por el módulo \englishText{NER} cuyo texto coincide exactamente con una
    entidad de referencia del caso.
}

\boldparagraph{Falsos positivos (\textit{false positives}, FP)}{
    entidades detectadas por el módulo \englishText{NER} que no coinciden con ninguna entidad de
    referencia, ya sea porque corresponden a un fragmento parcial de una entidad de referencia más
    larga o porque no guardan relación alguna con las entidades buscadas que se puede interpretar 
    como ruido clínico genérico.
}

\boldparagraph{Falsos negativos (\textit{false negatives}, FN)}{
    entidades de referencia del caso que no son cubiertas por ninguna de las entidades detectadas.
}

A partir de estos tres conteos se obtienen, para cada caso, las tres métricas reportadas en esta
subsección:

\begin{gather*}
    \text{Precisión} = \frac{\mathrm{TP}}{\mathrm{TP} + \mathrm{FP}} \\[8pt]
    \textit{Recall} = \frac{\mathrm{TP}}{\mathrm{TP} + \mathrm{FN}} \\[8pt]
    \mathrm{F1} = 2 \cdot \frac{\text{Precisión} \cdot \textit{Recall}}{\text{Precisión} + \textit{Recall}} \\[1pt]
\end{gather*}

donde la precisión mide qué proporción de las entidades detectadas son correctas, el \textit{recall}
mide qué proporción de las entidades de referencia fueron efectivamente detectadas, y el F1 resume
ambas en su media armónica. Estas tres métricas se reportan en dos variantes de promedio sobre los
10 casos: el promedio \textit{macro}, que calcula la métrica de cada caso por separado y luego
promedia esos 10 valores, y el promedio \textit{micro}, que primero suma los TP, FP y FN de los 10
casos y calcula las métricas una sola vez sobre esos totales agregados, dando así mayor peso a los
casos con más entidades de referencia.

Durante la ejecución de esta evaluación se realizaron las siguientes dos pruebas:

\boldparagraph{Evaluación estricta (entidad semántica completa)}{
    en una primera prueba, el \textit{gold standard} de cada caso se anotó exigiendo la
    entidad semántica completa de cada entidad clínica (p.\ ej.\
    \texttt{atrofia moderada de las vellosidades intestinales} como una única entidad). Bajo este
    criterio, el modelo obtuvo un F1 micro de 32,0\,\% (precisión 24,8\,\%, \textit{recall} 44,9\,\%),
    según el resumen de la Tabla~\ref{tab:ner_resumen_estricta} y el detalle completo por caso de la
    Tabla~\ref{tab:ner_detalle_estricta} (Anexo~\ref{anexo:resultados_ner_completos}). El análisis
    caso por caso reveló que la causa dominante de error no era la omisión total del contenido
    clínico, sino la \textbf{sobre-segmentación}: el modelo tiende a detectar fragmentos cortos
    dentro de una entidad de referencia más larga (p.\ ej.\ detecta \texttt{doloroso} en lugar de
    \texttt{cuadro doloroso}, o \texttt{leucocitos} en lugar de \texttt{16.000 leucocitos}), de modo
    que el fragmento se contabiliza como falso positivo y la entidad completa como falso negativo,
    penalizando dos veces el mismo error. A este patrón se suman dos fuentes de error adicionales:
    siglas y abreviaturas clínicas no reconocidas (\texttt{SDRC}, \texttt{HCA}, \texttt{DLE},
    \texttt{AOC}, \texttt{DG}, \texttt{RMN}) y un volumen elevado de falsos positivos por términos
    genéricos de exploración o síntomas parciales (\texttt{exploración física}, \texttt{dolor},
    \texttt{lesión}).
}

\input{tablas/capitulo_evaluacion/tabla_ner_resumen_estricta.tex}

% \boldparagraph{Evaluación granular (\textit{gold standard} fragmentado)}{
%     a partir del patrón de sobre-segmentación identificado en la evaluación estricta, se diseñó una
%     segunda prueba en la que el \textit{gold standard} se redefinió con mayor granularidad,
%     fragmentando las entidad de referencia en palabras más cortas que el modelo tiende a
%     producir de forma consistente (p.\ ej.\ \texttt{atrofia moderada} y
%     \texttt{vellosidades intestinales} como dos entidades independientes, en lugar de una sola). El
%     objetivo de esta segunda prueba es aislar qué proporción del F1 obtenido en la evaluación
%     estricta se explica por un criterio de delimitación de \textit{span} demasiado exigente, frente a
%     contenido clínico realmente no detectado por el modelo. Bajo este criterio más granular, el
%     modelo obtuvo un F1 micro de 43,8\,\% (precisión 37,3\,\%, \textit{recall} 53,1\,\%), según el
%     resumen de la Tabla~\ref{tab:ner_resumen_granular} y el detalle completo por caso de la
%     Tabla~\ref{tab:ner_detalle_granular} (Anexo~\ref{anexo:resultados_ner_completos}). Aun con este
%     criterio relajado, persisten los mismos tres errores estructurales identificados en la
%     evaluación estricta --- desalineación fina de límites de \textit{span}, ceguera ante siglas y
%     abreviaturas, y un volumen de falsos positivos genéricos prácticamente sin cambios (101 frente a
%     121) ---, lo que indica que estos no son artefactos del criterio de anotación sino limitaciones
%     reales del modelo.
% }

\boldparagraph{Evaluación granular (\textit{gold standard} fragmentado)}{
    A partir del patrón de sobresegmentación identificado en la evaluación estricta, se diseñó un 
    segundo experimento en el que el \textit{gold standard} se redefinió con mayor granularidad. Para 
    ello, las entidades de referencia se fragmentaron en las unidades más cortas que el modelo 
    tiende a predecir de forma consistente (por ejemplo, dividiendo \texttt{atrofia moderada vellosidades 
    intestinales} en dos entidades independientes: \texttt{atrofia moderada} y \texttt{vellosidades 
    intestinales}). 

    % El objetivo de esta prueba es determinar qué proporción de la caída en la métrica $F_1$ de la evaluación 
    % estricta se debe a un criterio de delimitación de \textit{span} excesivamente riguroso, en lugar de a 
    % un verdadero fallo en la detección del contenido clínico. 
    El objetivo de esta prueba es saber cuántos errores del modelo se debieron a un criterio de evaluación
    demasiado estricto (al exigir límites exactos en las palabras), y cuántos a una pérdida
    real de información clínica.

    Bajo este criterio granular, el modelo alcanzó un $F_1$ micro del 43,8\% (precisión: 37,3\%; \textit{recall}: 
    53,1\%), como se resume en la Tabla~\ref{tab:ner_resumen_granular} y se detalla por caso en la 
    Tabla~\ref{tab:ner_detalle_granular} (Anexo~\ref{anexo:resultados_ner_completos}). Sin embargo, aun con 
    este criterio flexible, persisten los mismos tres errores estructurales de la evaluación estricta: 
    la desalineación fina en los límites de los \textit{spans}, la incapacidad para reconocer siglas o 
    abreviaturas, y un volumen de falsos positivos genéricos casi idéntico (101 frente a 121). Esto demuestra 
    que dichos errores no son artefactos del criterio de anotación, sino limitaciones reales del modelo.
}

\input{tablas/capitulo_evaluacion/tabla_ner_resumen_granular.tex}

La Tabla~\ref{tab:ner_comparacion} resume la diferencia entre los promedios micro de ambas
pruebas. El F1 mejora 11,8 puntos porcentuales (de 32,0\,\% a 43,8\,\%) al pasar del criterio
estricto al granular, una mejora explicada principalmente por una reducción de 20 falsos
positivos (121 a 101) que pasan a sumarse como un verdaderos positivos al relajar la
exigencia de \textit{span} completo; el número de falsos negativos se mantiene prácticamente
estable (49 a 53), ya que el \textit{gold standard} granular contiene más entidades individuales
a cubrir. Esta diferencia permite estimar que aproximadamente un 37\,\% de la brecha de F1 
se explica por la granularidad con la que se delimitan los
\textit{spans} de las entidades, mientras que el 63\,\% restante corresponde a limitaciones reales
de la extracción: siglas y abreviaturas no reconocidas, valores numéricos con unidades no
capturados como una sola entidad, y un volumen elevado de falsos positivos por términos clínicos
genéricos. En ambas pruebas, el modelo se comporta de forma consistentemente mejor con
diagnósticos y fármacos expresados en una o dos palabras (p.\ ej.\ \texttt{hernia discal},
\texttt{azatioprina}) que con expresiones clínicas compuestas, acrónimos y cuantificaciones
numéricas, lo que señala estas últimas como las áreas prioritarias de mejora del módulo
\englishText{NER} de cara a un uso en producción.

\input{tablas/capitulo_evaluacion/tabla_ner_comparacion.tex}

\subsection{Normalización terminológica \englishText{SNOMED CT}/\englishText{CIE-10-ES}}\label{subsection:evaluacion_snomed}

\nota{Sección pendiente: completar con la evaluación cuantitativa individual del módulo de
normalización terminológica (p.\ ej. \% de entidades con mapeo correcto a SNOMED CT y CIE-10-ES
frente a la anotación de referencia), independiente de la evaluación end-to-end asistida por IA de la
Sección~\ref{section:evaluacion_e2e}.}

\section{Evaluación \englishText{end-to-end} asistida por IA}\label{section:evaluacion_e2e}

\subsection{Metodología y modalidades evaluadas}\label{subsection:metodologia_e2e}

Complementariamente a la evaluación por módulos de la Sección~\ref{section:evaluacion_modulos}, se
evaluó el sistema de extremo a extremo utilizando un \englishText{LLM} como juez automático
(\englishText{LLM-as-judge}). Para cada uno de los 10 casos de referencia, el modelo evaluador comparó
el texto de referencia frente a los tres artefactos de salida generados
por el sistema: el reporte de control enriquecido (\texttt{Reporte.txt}), el informe clínico
redactado por el \englishText{LLM} (\texttt{Informe.txt}) y el \englishText{Bundle FHIR}
(\texttt{Fhir\_Reporte.json}), asignando una puntuación de 1 a 5 en cinco dimensiones
(\englishText{ASR}, \englishText{NER}, terminología (\englishText{SNOMED CT} y \englishText{CIE-10-ES}), 
informe \englishText{LLM} y \englishText{FHIR}) y registrando el tiempo total de procesamiento de 
cada caso.

Esta evaluación se ejecutó por separado sobre las dos modalidades de funcionamiento previstas en el
diseño del sistema (Capítulo~\ref{chapter:desarrollo}):

\boldparagraph{Modo \textit{streaming}}{
    el \englishText{gateway} reenvía cada fragmento de audio de forma independiente al servicio
    \englishText{ASR} a medida que la aplicación móvil lo emite, generando y acumulando la información
    de transcripción, entidades y códigos terminológicos de forma incremental, fragmento a fragmento,
    durante toda la sesión de dictado.
}

\boldparagraph{Modo \textit{batch}}{
    el \englishText{gateway} almacena la totalidad de los fragmentos de audio recibidos durante la
    sesión y los envía como un único bloque consolidado al servicio \englishText{ASR} al finalizar el
    dictado, procesándose de una sola vez en lugar de incrementalmente.
}

\subsection{Comparación de resultados entre modalidades}\label{subsection:comparacion_e2e}

La Tabla~\ref{tab:e2e_comparacion_modos} resume las puntuaciones medias obtenidas en cada dimensión y
el tiempo total medio por caso para ambas modalidades, a partir de los informes completos de
evaluación de cada una (directorios \texttt{reports/mode\_batches/} y
\texttt{reports/mode\_streaming/}). El detalle caso por caso de ambas modalidades, a partir del cual
se calculan estos promedios, se recoge en las Tablas~\ref{tab:e2e_detalle_batch}
y~\ref{tab:e2e_detalle_streaming} del Anexo~\ref{anexo:resultados_e2e_completos}.

\input{tablas/capitulo_evaluacion/tabla_e2e_comparacion.tex}

\boldparagraph{Calidad de transcripción y extracción de entidades}{
    las puntuaciones de \englishText{ASR} (3.9 frente a 3.8) y \englishText{NER} (2.3 frente a 2.4) son
    prácticamente equivalentes entre modalidades, lo que confirma que la forma de agrupar y enviar los
    fragmentos de audio no afecta de forma apreciable a la calidad de estas dos primeras etapas del
    \englishText{pipeline}.
}

\boldparagraph{Terminológica (\englishText{SNOMED CT} y \englishText{CIE-10-ES})}{
    constituye la dimensión con mayor diferencia entre modalidades: 2.0 en \textit{batch} frente a 1.4
    en \textit{streaming}. En ambas modalidades coincide el mismo patrón de fallo --- mapeos léxicos sin
    desambiguación contextual que asocian términos cotidianos o ambiguos (p.\ ej. ``corrección'',
    ``doloroso'', ``amarillento'') a conceptos \englishText{SNOMED CT} clínicamente ajenos al dominio
    de la nota (especies animales o vegetales, síndromes genéticos raros, hallazgos psiquiátricos) ---,
    pero este patrón se manifiesta con mayor frecuencia e intensidad en el modo \textit{streaming}, en el
    que el porcentaje de casos con error semántico grave de terminología alcanza el 90\,\%
    (9 de 10 casos).
}

\boldparagraph{Informe \englishText{LLM}}{
    ambas modalidades obtienen puntuaciones bajas y similares (2.1 en \textit{batch}, 1.9 en
    \textit{streaming}), con dos modos de fallo recurrentes en los dos conjuntos de resultados: la
    alucinación de contenido no presente en la fuente (diagnósticos inventados, expansión de siglas,
    referencias bibliográficas fabricadas, errores de negación/polaridad) y el colapso de la tarea, en
    el que el modelo genera una meta-descripción del propio \englishText{pipeline} de codificación en
    lugar de un informe clínico estructurado.
}

\boldparagraph{Bundle \englishText{FHIR}}{
    obtiene puntuaciones equivalentes entre modalidades (2.7 en \textit{batch}, 2.6 en
    \textit{streaming}). En ambos casos el Bundle es estructuralmente válido en el 100\,\% de los casos,
    pero no filtra ni valida los códigos heredados de las etapas anteriores, propagando fielmente los
    errores de \englishText{NER} y terminología hacia recursos clínicos formales.
}

\boldparagraph{Tiempo total de procesamiento}{
    es la diferencia más marcada entre ambas modalidades: $\approx$32.1\,s de media por caso en modo
    \textit{batch} frente a $\approx$178.3\,s en modo \textit{streaming}, es decir, un tiempo medio
    aproximadamente 5.5 veces superior en \textit{streaming}. Esta diferencia no se explica por el
    módulo \englishText{ASR} --- rápido y estable en ambas modalidades ---, sino por el efecto acumulativo
    de ejecutar de forma independiente, para cada uno de los fragmentos de audio de la sesión, las
    etapas de \englishText{NER} y de traducción \englishText{CIE-10-ES}
    (\englishText{ConceptMap \$translate}), cuyo tiempo combinado domina el tiempo total en modo
    \textit{streaming} (p.\ ej.\ \texttt{Test6} y \texttt{Test10}, con $\approx$370\,s y más de 300\,s de
    \englishText{NER}+\englishText{ConceptMap} combinados). El modo \textit{batch}, al concentrar todo el
    audio en una sola invocación, evita esta repetición de llamadas y resulta sustancialmente más
    eficiente en tiempo total, a costa de no ofrecer retroalimentación incremental durante la sesión.
}

En conjunto, ambas modalidades presentan un patrón de fortalezas y debilidades equivalente ---
\englishText{ASR} sólido, \englishText{NER} y terminología como eslabones débiles, informe
\englishText{LLM} propenso a alucinaciones o colapso de formato, y \englishText{FHIR} estructuralmente
válido pero semánticamente ciego ---, lo que indica que estos problemas son inherentes a la lógica de
procesamiento compartida por ambas rutas y no a la estrategia de envío de audio. La modalidad
\textit{streaming} agrava ligeramente los errores de terminología y multiplica el tiempo total de
procesamiento, por lo que el modo \textit{batch} resulta preferible en escenarios donde no se requiera
retroalimentación incremental durante el dictado.

\section{Limitaciones metodológicas}\label{section:limitaciones_evaluacion}

\nota[Validación cualitativa con personal clínico]{
    el diseño original de la evaluación contemplaba contrastar la aceptación de los informes generados
    por el sistema con personal médico (objetivo específico~3, Sección~\ref{section:objetivos-especificos}),
    mediante la revisión directa de los informes y el conteo de ediciones menores requeridas. Esta
    validación no pudo realizarse dentro del alcance temporal de este TFM, por lo que el objetivo
    específico~3 no se evalúa empíricamente en este capítulo. Como aproximación, la evaluación
    \englishText{end-to-end} asistida por IA de la Sección~\ref{section:evaluacion_e2e} ofrece una
    estimación automática de la fidelidad y coherencia clínica del informe \englishText{LLM}, pero no
    sustituye el juicio de un profesional sanitario sobre la utilidad real del informe en un flujo de
    trabajo clínico.
}
