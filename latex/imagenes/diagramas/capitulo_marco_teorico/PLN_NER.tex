\begin{figure}[htbp]
    \centering
    \resizebox{\linewidth}{!}{%
    \begin{tikzpicture}[
        font=\sffamily,
        box/.style={
            draw,
            thick,
            align=center,
            inner xsep=16pt,
            inner ysep=24pt,
            text width=5.2cm,
            minimum height=3.8cm
        },
        side/.style={
            align=center,
            text width=3.8cm
        },
        arrow/.style={-{Stealth[scale=1.2]}, thick},
        lbl/.style={align=center, font=\large\bfseries}
    ]

    % Bloque central
    \node[box] (plnBox) {
        \Large\bfseries Procesamiento de Lenguaje\\
        \Large\bfseries Natural (PLN) y NER
    };

    % Bloque izquierdo: anclado al borde derecho, separado del central
    \node[side, left=1.8cm of plnBox] (inputGroup) {
        \begin{tikzpicture}
            \draw[thick, rounded corners=2pt] (-0.4, -0.5) rectangle (0.4, 0.6);
            \draw[thick] (-0.2, 0.3)  -- (0.2, 0.3);
            \draw[thick] (-0.2, 0.1)  -- (0.2, 0.1);
            \draw[thick] (-0.2, -0.1) -- (0.2, -0.1);
            \draw[thick] (-0.2, -0.3) -- (0.0, -0.3);
        \end{tikzpicture} \\[8pt]
        \large Hoy, María visitó la tienda de Apple en Madrid para comprar un iPhone.
    };

    \node[lbl, above=0.6cm of inputGroup] (inputTitle) {Texto de Entrada};

    % Bloque derecho: anclado al borde izquierdo, separado del central
    \node[side, align=left, text width=4.6cm, right=1.8cm of plnBox] (outputTxt) {
        \large [María]\textbf{\colorPrincipal{Persona}} visitó la tienda
        de [Apple]\textbf{\colorPrincipal{Organización}} en
        [Madrid]\textbf{\colorPrincipal{Ubicación}} para
        comprar un [iPhone]\textbf{\colorPrincipal{Producto}}.
    };

    \node[lbl, above=0.6cm of outputTxt] (outputTitle) {Resultado de NER};

    % Conexiones entre bordes (sin invadir la caja central)
    \draw[arrow] (inputGroup.east) -- (plnBox.west);
    \draw[arrow] (plnBox.east) -- (outputTxt.west);

    \end{tikzpicture}%
    }
    \caption{Diagrama funcional corregido para el proceso de NER.}
    \label{fig:diagrama_ner_corregido}
\end{figure}
